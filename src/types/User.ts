export type UserData = {
    name: string,
    email: string,
    password: string
}

export type ContactData ={
    name:string,
    email:string,
    message:string
}

export type LoginData = {
    email: string,
    password: string
}
